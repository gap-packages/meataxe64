#!/bin/bash
rm -f pid.txt
rm -f log.txt
sh $BIN/run1.sh $* &
echo "Starting $*" `date` > reclog
while :
do
    sleep 2
    date >> reclog
    if ! ps -p `cat pid.txt` -o "cputime= rss=" | tee -a reclog | grep "" > /dev/null; then
       exit;
    fi
    sleep 4
done
